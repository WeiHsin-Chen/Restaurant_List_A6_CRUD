<header>
  <!-- navigation -->
  <nav class="navbar navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">
        <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
          width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單">
        {{#if isAuthenticated}}{{ user.name}}'s {{/if}}餐廳清單
      </a>
      <div class="row">
        {{#if isAuthenticated}}
        <a href="../../restaurants/new" class="mr-3">
          <button class="navbar-create" type="submit" class="d-inline-block">新增餐廳</button>
        </a>
        {{/if}}
        {{#if isAuthenticated}}
        <a href="/users/logout">
          <button class="navbar-create" type="submit" class="d-inline-block">Logout</button>
        </a>
        {{else}}
        <a href="/users/login">
          <button class="navbar-create" type="submit" class="d-inline-block">Login</button>
        </a>
        {{/if}}
      </div>
    </div>
  </nav>

  <div class="banner"></div>
  <!-- link css files here -->
  <link rel="stylesheet" href="https://bootswatch.com/4/lumen/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/index.css">
</header>

<body>
  <!-- partial templates will replace the part of "body" here -->
  {{{ body }}}

  <!-- include javascript files here -->
  <script src="/javascripts/jquery-3.3.1.js"></script>
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
  <script src="https://kit.fontawesome.com/fd7b48982d.js" crossorigin="anonymous"></script>
</body>